const appConfig = window.appConfig;

export const OAUTH_CLIENT_ID = appConfig?.OAUTH_CLIENT_ID;
export const OAUTH_REDIRECT_URI = appConfig?.OAUTH_REDIRECT_URI;
export const ANALYTICS_URL = appConfig?.ANALYTICS_URL;